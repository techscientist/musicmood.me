build:
  image: ubuntu:14.04
  commands:
    - curl -sL https://deb.nodesource.com/setup_5.x | sudo -E bash -
    - sudo apt-get install nodejs
    - cd node
    - npm install
    - echo '{}' > config.json
    - mkdir .nyc_output
    - npm run coverage
